# Service gallery folders

## Corporate Photography — Gallery preview (6 images)

Upload to:

```
public/images/gallery/corporate-photography/
```

Shows the **6 most recently modified** files in the **Gallery preview** section on `/photography/corporate-photography`.

## Corporate Photography — Masonry grid (Load more)

Upload to:

```
public/images/gallery/corporate-photography/masonry/
```

Powers the **large 4-column gallery** after the Ready to Connect section. All images in this folder are included, sorted newest first.

## Event Photography — Gallery preview (6 images)

Upload to:

```
public/images/gallery/event-photography/
```

Shows the **6 most recently modified** files in the **Gallery preview** section on `/photography/event-photography`.

Only files in this folder root are used (not the `masonry/` subfolder).

## Event Photography — Masonry grid (Load more)

Upload to:

```
public/images/gallery/event-photography/masonry/
```

Powers the **large 4-column gallery** further down the same page.

## Wedding Photography — Gallery preview (6 images)

Upload to:

```
public/images/gallery/wedding-photography/
```

Shows the **6 most recently modified** files in the **Gallery preview** section on `/photography/wedding-photography`.

Only files in this folder root are used (not the `masonry/` subfolder).

## Wedding Photography — Masonry grid (Load more)

Upload to:

```
public/images/gallery/wedding-photography/masonry/
```

Powers the **large 4-column gallery** further down the same page. All images in this folder are included, sorted newest first. The existing **Load more** button reveals them in batches.

## Portrait Photography — Gallery preview (6 images)

Upload to:

```
public/images/gallery/portrait-photography/
```

Shows the **6 most recently modified** files in the **Gallery preview** section on `/photography/portrait-photography`.

Only files in this folder root are used (not the `masonry/` subfolder).

## Portrait Photography — Masonry grid (Load more)

Upload to:

```
public/images/gallery/portrait-photography/masonry/
```

Powers the **large 4-column gallery** further down the same page.

## Fashion & Model Photography — Gallery preview (6 images)

Upload to:

```
public/images/gallery/fashion-model-photography/
```

Shows the **6 most recently modified** files in the **Gallery preview** section on `/photography/fashion-model-photography`.

Only files in this folder root are used (not the `masonry/` subfolder).

## Fashion & Model Photography — Masonry grid (Load more)

Upload to:

```
public/images/gallery/fashion-model-photography/masonry/
```

Powers the **large 4-column gallery** further down the same page.

## Product Photography — Gallery preview (6 images)

Upload to:

```
public/images/gallery/product-photography/
```

Shows the **6 most recently modified** files in the **Gallery preview** section on `/photography/product-photography`.

Only files in this folder root are used (not the `masonry/` subfolder).

## Product Photography — Masonry grid (Load more)

Upload to:

```
public/images/gallery/product-photography/masonry/
```

Powers the **large 4-column gallery** further down the same page.

## Real Estate Photography — Gallery preview (6 images)

Upload to:

```
public/images/gallery/real-estate-photography/
```

Shows the **6 most recently modified** files in the **Gallery preview** section on `/photography/real-estate-photography`.

Only files in this folder root are used (not the `masonry/` subfolder).

## Real Estate Photography — Masonry grid (Load more)

Upload to:

```
public/images/gallery/real-estate-photography/masonry/
```

Powers the **large 4-column gallery** further down the same page.

## Drone Photography — Gallery preview (6 images)

Upload to:

```
public/images/gallery/drone-photography/
```

Shows the **6 most recently modified** files in the **Gallery preview** section on `/photography/drone-photography`.

Only files in this folder root are used (not the `masonry/` subfolder).

## Drone Photography — Masonry grid (Load more)

Upload to:

```
public/images/gallery/drone-photography/masonry/
```

Powers the **large 4-column gallery** further down the same page.

## Formats

Supported: `.webp`, `.jpg`, `.jpeg`, `.png`, `.avif`  
Recommended: `.webp`

## Naming

Use descriptive filenames for accessibility, e.g. `01-ceremony.webp`, `02-reception.webp`. Alt text is generated from the filename.

No code changes needed after upload — refresh the page (production revalidates every ~60 seconds).
